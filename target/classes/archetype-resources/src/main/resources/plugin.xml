#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8"?>
<plugin>
   <extension point="com.change_vision.astah.ui.actionSets">
      <actionSet
            label="プラグインテンプレートアクション・セット"
            visible="true"
            id="${groupId}.${artifactId}.actionSet">
         <menu
               id="${artifactId}"
               label="テンプレートグループ"
               path="tool/${artifactId}">
         </menu>
         <action
			   label="プラグインテンプレート(&amp;T)"
               icon="icons/sample.gif"
               class="${package}.TemplateAction"
               tooltip="プラグインテンプレート"
               menubarPath="tool/${artifactId}/"
               id="${artifactId}">
         </action>
     </actionSet>
   </extension>
</plugin>
